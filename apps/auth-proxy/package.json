{
  "name": "@pixstudy/auth-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "clean": "rm -rf .turbo node_modules",
    "dev": "nitro dev --port 3001",
    "lint": "eslint .",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@auth/core": "^0.18.0"
  },
  "devDependencies": {
    "@pixstudy/eslint-config": "workspace:^0.2.0",
    "@pixstudy/prettier-config": "workspace:^0.1.0",
    "@pixstudy/tailwind-config": "workspace:^0.1.0",
    "@pixstudy/tsconfig": "workspace:^0.1.0",
    "eslint": "^8.53.0",
    "nitropack": "^2.7.2",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "eslintConfig": {
    "root": true,
    "extends": [
      "@pixstudy/eslint-config/base"
    ]
  },
  "prettier": "@pixstudy/prettier-config"
}